set -g default-terminal "tmux-256color"
set-option -a terminal-features "tmux-256color:RGB"

# Undercurl support
set -as terminal-overrides ',*:Smulx=\E[4::%p1%dm'  # undercurl support
set -as terminal-overrides ',*:Setulc=\E[58::2::%p1%{65536}%/%d::%p1%{256}%/%{255}%&%d::%p1%{255}%&%d%;m'  # underscore colours - needs tmux-3.0

### Basic setting
unbind C-b
set-option -g prefix C-a
set -sg escape-time 10
set -g mouse on

### Window and Pane settings
set -g renumber-windows on # make sure the window numbers are always sequential without gaps
set -g base-index 1 # start with window number 1
setw -g monitor-activity on # notify if there is an activity in an inative window


### Key bindings

# split panes using | and -
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"
unbind '"'
unbind %
unbind i

# make copy mode more vim-like
bind-key -T copy-mode-vi v send-keys -X begin-selection
# bind-key -T copy-mode-vi C-v send-keys -X rectangle-toggle
bind-key -T copy-mode-vi 'C-v' send -X begin-selection \; send -X rectangle-toggle

# new window with the same cwd as current window
bind-key c new-window -c "#{pane_current_path}"

# kill pane without confirmation
unbind-key x
bind-key x kill-pane

# resize panes with vim keys
bind-key -r C-h resize-pane -L 1
bind-key -r C-j resize-pane -D 1
bind-key -r C-k resize-pane -U 1
bind-key -r C-l resize-pane -R 1


bind-key -r M-h resize-pane -L 5
bind-key -r M-j resize-pane -D 5
bind-key -r M-k resize-pane -U 5
bind-key -r M-l resize-pane -R 5

# move between panes with vim keys
bind-key -r h select-pane -L
bind-key -r j select-pane -D
bind-key -r k select-pane -U
bind-key -r l select-pane -R

# last window
bind-key Tab last-window

# Project switcher popup (tm)
bind-key T display-popup -E -w 80% -h 80% "fish -c tm"

# Session switcher, overriding the default binding only to add attention and agent
# markers (see attention/README.md). The three branches mirror tmux's own default
# format: choose-tree evaluates one format for every row and #{?pane_format,...} /
# #{?window_format,...} is how a row says which kind it is. Sessions stay collapsed,
# so the session-level rollup is what makes a waiting agent visible without
# expanding the tree.
bind-key s choose-tree -Zs -F "#{?pane_format,#{pane_current_command}#{?#{@att_state}, #{E:@att_marker_pane},}#{?#{@att_agent}, #{E:@att_marker_agent_pane},},#{?window_format,#{window_name}#{window_flags}#{?#{@att_win}, #{E:@att_marker_win},}#{?#{@att_agent_win}, #{E:@att_marker_agent_win},},#{session_name}: #{session_windows} windows#{?session_attached, (attached),}#{?#{@att_sess}, #{E:@att_marker_sess},}#{?#{@att_agent_sess}, #{E:@att_marker_agent_sess},}}}"

### Colors and Styles

# Tokyonight night palette, matching the ghostty theme. The bar sits one step
# darker than the terminal background (#1a1b26) so it reads as a recessed
# surface instead of competing with pane content.
set -g status-style "bg=#16161e,fg=#787c99"
set -g status-justify left

set -g status-left "#[fg=#7aa2f7,bold] #S #[default]"
set -g status-left-length 30

# The leading title fragment is copied verbatim from tmux's default status-right:
# the quotes are literal, and the conditional shows the viewport offset only when
# the window is larger than the client. `#,` is an escaped comma, which would
# otherwise terminate the conditional's true branch.
#
# #{prefix_highlight} is the placeholder tmux-prefix-highlight substitutes into;
# without it here the plugin has nothing to attach to and silently does nothing.
set -g status-right "#{?window_bigger,[#{window_offset_x}#,#{window_offset_y}] ,}\"#{=21:pane_title}\" #{prefix_highlight}#[fg=#565f89]%H:%M #[fg=#787c99] #h "
set -g status-right-length 70

### Attention markers (see attention/README.md)

# attentiond writes the state name and a count; the colours stay here so the theme
# is edited in one place. #{E:...} expands an option's value as a format, which is
# what lets one marker definition serve both the status line and choose-tree.
#
# Rollups show the highest-severity state in the window or session -- blocked
# outranks error because blocked is the only state where you are the bottleneck --
# and append a count only when more than one pane shares it.
#
# Fill carries how much is pending and hue carries what kind: ◉ ringed for blocked,
# ● solid for a finished turn, ○ hollow for an agent sitting idle. A triangle stays
# on error because a failed command is not a degree of the same thing.
set -g @att_marker_win "#{?#{==:#{@att_win},blocked},#[fg=#f7768e]◉,#{?#{==:#{@att_win},error},#[fg=#e0af68]▲,#{?#{==:#{@att_win},done},#[fg=#73daca]●,}}}#{?#{>:#{@att_win_n},1},#{@att_win_n},}"
set -g @att_marker_sess "#{?#{==:#{@att_sess},blocked},#[fg=#f7768e]◉,#{?#{==:#{@att_sess},error},#[fg=#e0af68]▲,#{?#{==:#{@att_sess},done},#[fg=#73daca]●,}}}#{?#{>:#{@att_sess_n},1},#{@att_sess_n},}"
set -g @att_marker_pane "#{?#{==:#{@att_state},blocked},#[fg=#f7768e]◉,#{?#{==:#{@att_state},error},#[fg=#e0af68]▲,#{?#{==:#{@att_state},done},#[fg=#73daca]●,}}}"

# The agent axis: what the agent in the pane is doing, which is not cleared by
# looking at it. These live in choose-tree only -- the status line deliberately
# says nothing about it, so the bar keeps meaning "something wants you" and stays
# empty the rest of the time.
#
# `working` and `idle` take the solid and hollow dots; `waiting` keeps a diamond
# rather than borrowing blocked's ◉, because the two land on one row together --
# they are set by the same event, and only blocked clears when you visit, so the
# row has to stay readable in both halves of that.
set -g @att_marker_agent_win "#{?#{==:#{@att_agent_win},waiting},#[fg=#f7768e]◆,#{?#{==:#{@att_agent_win},working},#[fg=#e0af68]●,#{?#{==:#{@att_agent_win},idle},#[fg=#565f89]○,}}}#{?#{>:#{@att_agent_win_n},1},#{@att_agent_win_n},}"
set -g @att_marker_agent_sess "#{?#{==:#{@att_agent_sess},waiting},#[fg=#f7768e]◆,#{?#{==:#{@att_agent_sess},working},#[fg=#e0af68]●,#{?#{==:#{@att_agent_sess},idle},#[fg=#565f89]○,}}}#{?#{>:#{@att_agent_sess_n},1},#{@att_agent_sess_n},}"
set -g @att_marker_agent_pane "#{?#{==:#{@att_agent},waiting},#[fg=#f7768e]◆,#{?#{==:#{@att_agent},working},#[fg=#e0af68]●,#{?#{==:#{@att_agent},idle},#[fg=#565f89]○,}}}"

# On the status line the state recolours the window entry instead of adding a glyph,
# so the bar keeps one visual vocabulary. Bold still means "current" and colour means
# state, which cannot collide in practice: a window you are looking at has its state
# cleared within a second, so "current and blocked" does not persist.
#
# Hue carries the urgency -- red for blocked, yellow for error, teal for done --
# which is why blocked needs no extra weight to read as the loud one. It is the same
# hue per state as the markers above, so a state does not change colour depending on
# which surface you read it from. The commas inside #[...] are escaped as #, because
# an unescaped one would end the conditional branch it sits in.
set -g @att_style_win "#{?#{==:#{@att_win},blocked},#[fg=#f7768e],#{?#{==:#{@att_win},error},#[fg=#e0af68],#{?#{==:#{@att_win},done},#[fg=#73daca],}}}"

setw -g window-status-format "#[fg=#565f89]#{?#{@att_win},#{E:@att_style_win},} #I #W "
setw -g window-status-current-format "#[fg=#7aa2f7,bold]#{?#{@att_win},#{E:@att_style_win}#[bold],} #I #W "
setw -g window-status-activity-style "fg=#e0af68,none"
setw -g window-status-separator ""

set -g @prefix_highlight_fg '#16161e'
set -g @prefix_highlight_bg '#e0af68'

# Since tmux 3.7 the prompt is drawn over the status line rather than replacing
# it, and only `fill` clears what is underneath; without it the bar shows
# through whatever is typed at the prompt.
set -g message-style "bg=#24283b,fg=#c0caf5,fill=#24283b"
set -g message-command-style "bg=#24283b,fg=#7aa2f7,fill=#24283b"

set -g mode-style "bg=#3b4261,fg=#c0caf5"
set -g pane-border-style "fg=#292e42"
set -g pane-active-border-style "fg=#3d59a1"

# pane number display
set-option -g display-panes-active-colour colour33 #blue
set-option -g display-panes-colour colour166 #orange

# clock
set-window-option -g clock-mode-colour colour64 #green

set-option -g set-titles on
set-option -g set-titles-string "#T"


# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-prefix-highlight'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'

# tmux-continuum & tmux-resurrecrt
set -g @continuum-restore 'off'
set -g @resurrect-capture-pane-contents 'on'
set -g @resurrect-strategy-nvim 'session'

setenv -g TMUX_PLUGIN_MANAGER_PATH "$HOME/.tmux/plugins/"
if "test ! -d ~/.tmux/plugins/tpm" \
   "run 'git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm && ~/.tmux/plugins/tpm/bin/install_plugins'"
# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run -b '~/.tmux/plugins/tpm/tpm'
